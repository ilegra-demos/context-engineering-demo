package com.example.demo;

import org.junit.jupiter.api.DisplayName;

@DisplayName("Verify product catalog UI structure and adding a new product with discount via CLI workflow")
public class ProductCatalogCliE2eTest extends ProductCatalogE2eTest {
    // Inherits all test execution logic from ProductCatalogE2eTest to avoid code duplication
}
